{{- if not .Values.keyVault.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: sonarsniffer-secrets
  labels:
    app.kubernetes.io/managed-by: "Helm"
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  SENTRY_DSN: ""
{{- end }}
