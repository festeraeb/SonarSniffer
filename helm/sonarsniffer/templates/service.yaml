apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonarsniffer.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "sonarsniffer.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
