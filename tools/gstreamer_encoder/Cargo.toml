[package]
name = "gst_encoder"
version = "0.1.0"
edition = "2021"

[dependencies]
gstreamer = "0.21"
gstreamer-app = "0.21"
gstreamer-video = "0.21"
clap = { version = "4", features = ["derive"] }
image = "0.24"

[profile.release]
opt-level = 3
